<ul>
  {{#each users}}
    <li>
      <h2>Usuario: {{email}}</h2>
      <p>Role: {{role}}</p>

      <a href="/api/users/changeRole/{{_id}}">Cambiar rol</a>
      <a href="/api/users/deleteUser/{{_id}}">Eliminar usuario</a>
    </li>
  {{/each}}
</ul>